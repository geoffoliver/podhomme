# Optional: restrict access with a shared password. Leave blank for open access.
APP_PASSWORD=

# Required when APP_PASSWORD is set: random string the worker uses to call the app API.
WORKER_SECRET=

# Your app's public URL. Used by the BeardedSpice endpoint.
WEB_URL=http://localhost:3030
